spring:
  datasource:
    url: jdbc:mysql://localhost:3306/scpa_dev?useSSL=false&&createDatabaseIfNotExist=true
    username: scpa_dev_user
    password: Pq4s67Xa
    platform: mysql
  jpa:
    hibernate:
      ddl-auto: update
      statistics: true
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql


     
 #  show-sql: true
 #   properties:
  #     javax:
   #      persistence:
    #      schema-generation:
     #       create-source: metadata
      #      scripts:
       #       action: create
        #      create-target: database_create.sql